name: CI/CD Deployment - Self-Hosted VM (No Registry)

on:
  push:
    branches:
      - main
  workflow_dispatch:

env:
  CONSUMER_IMAGE: post-consumer:latest
  PUSHER_IMAGE: post-pusher-kafka:latest
  SA_SECRET_NAME: gcp-sa-secret
  KIND_CLUSTER_NAME: devops-kind-cluster # Nom par défaut pour le cluster Kind
  
jobs:
  build-and-deploy-on-vm:
    runs-on: self-hosted 
    
    steps:
      - name: 📥 Checkout Code
        uses: actions/checkout@v4

      # --- 1. CONFIGURATION DU CLUSTER KIND (NOUVELLE SECTION) ---
      
      - name: ⚙️ Setup Kind and Start Cluster
        run: |
          # 1. Vérifie si le cluster existe.
          
          if kind get clusters | grep -q "${{ env.KIND_CLUSTER_NAME }}"; then
            echo "Cluster Kind '${{ env.KIND_CLUSTER_NAME }}' existe déjà. Utilisation de l'existant."
          else
            echo "Cluster Kind '${{ env.KIND_CLUSTER_NAME }}' n'existe pas. Création en cours..."
            kind create cluster --name ${{ env.KIND_CLUSTER_NAME }} --config ./kind/config.yaml
          fi

          # 2. Récupération et encodage du Kubeconfig (Nécessaire dans tous les cas pour les actions Azure)
          KIND_KUBECONFIG=$(kind get kubeconfig --name ${{ env.KIND_CLUSTER_NAME }} --internal)
          
          # Correction EOF pour les variables multi-lignes
          echo 'KUBECONFIG_CONTEXT<<EOF' >> $GITHUB_ENV
          echo "$KIND_KUBECONFIG" >> $GITHUB_ENV
          echo 'EOF' >> $GITHUB_ENV

      - name: Configure Kubectl (Installer l'exécutable)
        uses: azure/setup-kubectl@v3
        with:
          version: 'latest'

      - name: 🔗 Set K8s Context for Azure Actions
        uses: azure/k8s-set-context@v3
        with:
          method: kubeconfig
          # Passe le KUBECONFIG généré par Kind
          kubeconfig: ${{ env.KUBECONFIG_CONTEXT }}
          # Le nom de contexte par défaut pour Kind est 'kind-NOM-DU-CLUSTER'
          cluster-context: kind-${{ env.KIND_CLUSTER_NAME }}
          
      # --- 2. CONSTRUCTION DES IMAGES (Aucun changement) ---
      
      - name: 🔨 Build and Load Consumer Image
        uses: docker/build-push-action@v5
        with:
          context: ./post_consumer
          file: ./post_consumer/Dockerfile
          push: false 
          load: true 
          tags: ${{ env.CONSUMER_IMAGE }}
          
      - name: 🔨 Build and Load Pusher Image
        uses: docker/build-push-action@v5
        with:
          context: ./post_pusher
          file: ./post_pusher/Dockerfile
          push: false
          load: true
          tags: ${{ env.PUSHER_IMAGE }}

      # --- 3. DÉPLOIEMENT (Utilisera le contexte Kind défini) ---

      - name: 🔑 Create/Update Secret for Service Account (GCP)
        uses: azure/k8s-create-secret@v4
        with:
          namespace: default
          secret-type: 'generic'
          secret-name: ${{ env.SA_SECRET_NAME }}
          string-data:  |
            service-account.json=${{ secrets.SA_JSON }}

      - name: 🚀 Deploy Kafka Infrastructure
        uses: azure/k8s-deploy@v4
        with:
          namespace: default
          manifests: |
            kafka/configmap.yaml
            kafka/service.yaml
            kafka/deployment.yaml

      - name: 🚀 Deploy Kafka UI
        uses: azure/k8s-deploy@v4
        with:
          namespace: default
          manifests: |
            ui/service.yaml
            ui/deployment.yaml

      - name: 🚀 Deploy Application Deployments
        uses: azure/k8s-deploy@v4
        with:
          namespace: default
          manifests: |
            post_consumer/deployment.yaml
            post_pusher/deployment.yaml
          images: |
            ${{ env.CONSUMER_IMAGE }}
            ${{ env.PUSHER_IMAGE }}
            
      - name: ✅ Deployment Complete
        run: echo "Le déploiement est terminé. Les images sont locales sur la VM."