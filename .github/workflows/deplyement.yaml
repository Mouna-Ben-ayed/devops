name: CI/CD Deployment - Self-Hosted VM (No Registry)

on:
  push:
    branches:
      - main
  workflow_dispatch:

env:
  CONSUMER_IMAGE: post-consumer:latest
  PUSHER_IMAGE: post-pusher-kafka:latest
  SA_SECRET_NAME: gcp-sa-secret
  KIND_CLUSTER_NAME: devops-kind-cluster
  
jobs:
  build-and-deploy-on-vm:
    runs-on: self-hosted
    
    steps:
      - name: 📥 Checkout Code
        uses: actions/checkout@v4

      # --- 1. CONFIGURATION DU CLUSTER KIND (CORRECTION DNS ET KUBECONFIG ESSENTIELLE) ---
      
      - name: ⚙️ Setup Kind and Start Cluster
        run: |
          # 1. Création du cluster uniquement s'il n'existe pas.
          if kind get clusters | grep -q "${{ env.KIND_CLUSTER_NAME }}"; then
            echo "Cluster Kind '${{ env.KIND_CLUSTER_NAME }}' existe déjà. Utilisation de l'existant."
          else
            kind create cluster --name ${{ env.KIND_CLUSTER_NAME }} --config ./kind/config.yaml
          fi

          # 2. Obtention du Kubeconfig brut (avec nom d'hôte interne)
          KIND_KUBECONFIG=$(kind get kubeconfig --name ${{ env.KIND_CLUSTER_NAME }} --internal)
          
          # 3. CORRECTION DNS : Obtenir l'IP du conteneur Kind depuis l'hôte
          #    (Résout l'erreur 'no such host'.)
          CONTROL_PLANE_IP=$(docker inspect -f '{{range .NetworkSettings.Networks}}{{.IPAddress}}{{end}}' "${{ env.KIND_CLUSTER_NAME }}-control-plane")
          
          # 4. Remplacer le nom d'hôte interne par l'adresse IP et le port 6443.
          KIND_KUBECONFIG_FIXED=$(echo "$KIND_KUBECONFIG" | sed "s/server: https:\/\/.*/server: https:\/\/$CONTROL_PLANE_IP:6443/")
          
          # 5. Injecter le Kubeconfig corrigé (avec l'IP) en utilisant la syntaxe EOF (multi-lignes).
          echo 'KUBECONFIG_CONTEXT<<EOF' >> $GITHUB_ENV
          echo "$KIND_KUBECONFIG_FIXED" >> $GITHUB_ENV
          echo 'EOF' >> $GITHUB_ENV

      - name: Configure Kubectl
        uses: azure/setup-kubectl@v3
        with:
          version: 'latest'

      - name: 🔗 Set Kubernetes Context
        uses: azure/k8s-set-context@v3
        with:
          method: kubeconfig
          # Passe le CONTENU du Kubeconfig corrigé
          kubeconfig: ${{ env.KUBECONFIG_CONTEXT }}

      # --- 2. BUILD DES IMAGES DOCKER ---
      - name: 🔨 Build and Load Consumer Image
        uses: docker/build-push-action@v5
        with:
          context: ./post_consumer
          file: ./post_consumer/Dockerfile
          push: false
          load: true
          tags: ${{ env.CONSUMER_IMAGE }}

      - name: 🔨 Build and Load Pusher Image
        uses: docker/build-push-action@v5
        with:
          context: ./post_pusher
          file: ./post_pusher/Dockerfile
          push: false
          load: true
          tags: ${{ env.PUSHER_IMAGE }}

      - name: Wait for Kubernetes API
        # Kubectl utilise désormais le contexte KUBECONFIG_CONTEXT corrigé
        run: kubectl wait --for=condition=ready node --all --timeout=300s

      # --- 3. SECRET GCP (CORRECTION: UTILISATION DE --FROM-FILE) ---
      - name: 🔑 Create/Update GCP Service Account Secret
        run: |
          # Méthode la plus sûre: Création d'un fichier temporaire
          echo "${{ secrets.SA_JSON }}" > sa-gcp.json
          
          kubectl create secret generic ${{ env.SA_SECRET_NAME }} \
            --from-file=service-account.json=sa-gcp.json \
            --namespace=default \
            --dry-run=client -o yaml | kubectl apply -f -
            
          rm sa-gcp.json

      # --- 4. DEPLOY KUBERNETES MANIFESTS ---
      - name: 🚀 Deploy Kafka Infrastructure
        uses: azure/k8s-deploy@v4
        with:
          namespace: default
          manifests: |
            kafka/configmap.yaml
            kafka/service.yaml
            kafka/deployment.yaml

      - name: 🚀 Deploy Kafka UI
        uses: azure/k8s-deploy@v4
        with:
          namespace: default
          manifests: |
            ui/service.yaml
            ui/deployment.yaml

      - name: 🚀 Deploy Application Deployments
        uses: azure/k8s-deploy@v4
        with:
          namespace: default
          manifests: |
            post_consumer/deployment.yaml
            post_pusher/deployment.yaml
          images: |
            ${{ env.CONSUMER_IMAGE }}
            ${{ env.PUSHER_IMAGE }}

      - name: ✅ Deployment Complete
        run: echo "Déploiement terminé. Les images sont locales sur la VM."